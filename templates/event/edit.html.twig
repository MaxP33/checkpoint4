{% extends 'base.html.twig' %}

{% block title %}Editer un évènement{% endblock %}

{% block body %}
    <h1>Editer un évènement</h1>

    <div class="d-flex">
        {{ include('event/_form.html.twig', {'button_label': 'Modifier'}) }}
    </div>
    <div class="d-flex mt-4">
        {{ include('event/_delete_form.html.twig') }}
        <a class="btn ml-3" href="{{ path('event_index') }}">Liste des évènements</a>
    </div>
{% endblock %}
